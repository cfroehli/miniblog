.h1  Most liked posts as of today

%p
  Dear user #{@user.username}, please find below the new posts ranking

  %table{cellpadding: "0", cellspacing: "0", border: "0", align: "center"}
    %thead
      %tr
        %th{width: "50", valign: "top"} Likes
        %th{width: "450", valign: "top"} Headline
        %th{width: "100", valign: "top"} User
        %th{width: "200", valign: "top"} Updated
    %tbody
      - @posts.each do |post|
        %tr
          %td
            = post.likes.size
            %img.far.fa-heart
          %td
            = link_to post do
              = headline(post)
          %td
            = post.user_name
          %td
            = post.updated_at.to_s(:update_timestamp)
